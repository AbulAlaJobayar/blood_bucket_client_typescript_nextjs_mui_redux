export enum tagType {
        donor = "donor",
        request = "request",
        auth = "auth",
        
      }
      export const tagTypeList = [
        tagType.donor,
        tagType.request,
        tagType.auth
       
      ];